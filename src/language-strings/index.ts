import { en } from './en';
import { pt } from './pt';

export const languageStringsByLanguage: Record<string, Record<string, any>> = {
    en,
    pt,
}

export const languages = {
    ENGLISH: 'en',
    PORTUGUESE: 'pt',
}
